import 'package:flutter/material.dart';

class HomeTabletBody extends StatelessWidget {
  const HomeTabletBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tablet"),
    );
  }
}
