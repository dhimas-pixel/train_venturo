import 'package:flutter/material.dart';

class OrderTabletBody extends StatelessWidget {
  const OrderTabletBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tablet"),
    );
  }
}
