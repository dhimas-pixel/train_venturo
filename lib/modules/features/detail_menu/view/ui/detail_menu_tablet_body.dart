import 'package:flutter/material.dart';

class DetailMenuTabletBody extends StatelessWidget {
  const DetailMenuTabletBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tablet"),
    );
  }
}
