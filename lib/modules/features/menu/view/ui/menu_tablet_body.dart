import 'package:flutter/material.dart';

class MenuTabletBody extends StatelessWidget {
  const MenuTabletBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tablet"),
    );
  }
}
